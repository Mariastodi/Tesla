services:
  - type: web
    name: projeto-tesla
    runtime: node
    plan: free
    buildCommand: npm ci --include=dev && npm run build
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: NODE_VERSION
        value: 22.14.0
      - key: TRUST_PROXY_HOPS
        value: 1
      - key: DATABASE_URL
        sync: false
      # Keep the existing credential until ADMIN_PIN_HASH is configured privately.
      - key: ADMIN_PIN
        generateValue: true
      - key: ADMIN_PIN_HASH
        sync: false
      - key: CPF_SALT
        generateValue: true
      - key: DEVICE_SECRET
        generateValue: true
